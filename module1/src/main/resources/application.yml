#database
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/service1
    username: user
    password: 123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

#server
server:
  port: 9000