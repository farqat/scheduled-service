#server
server:
  port: 9001

#database
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/service2
    username: user
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect